// Create a class Cube and a class Sphere with private data members side and
// radius respectively. Both should also contain a private variable volume
// indicating the volume of Cube and Sphere.
// Use suitable constructors to initialize the instance variables. Write suitable
// functions required to return (a) positive if the volume of Cube object is less
// than the Sphere object
// (b) 0 if their volumes are the same
// (c ) Negative otherwise.

public class CubeSphere {
    
}
